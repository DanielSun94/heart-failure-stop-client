const initialState = {
    algorithmList: []
};

const algorithmReducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};

export default algorithmReducer;
