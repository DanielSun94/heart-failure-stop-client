export const eventMeaning =()=>{
    return (
        [
            ['糖尿病入院' , ''],
            ['肾病入院' , ''],
            ['其它' , ''],
            ['心功能1级' , ''],
            ['心功能2级' , ''],
            ['心功能3级' , ''],
            ['心功能4级' , ''],
            ['死亡' , ''],
            ['再血管化手术' , ''],
            ['癌症' , ''],
            ['肺病' , '']
        ]
    )
};

export const featureMeaning =()=>{return [
    ['心源性入院' , ''],
    ['性别' , '男1，女0'],
    ['主动脉球囊反搏术' , '本次入院是否使用'],
    ['瓣膜置换' , '本次入院是否使用'],
    ['植入心脏起搏器' , '本次入院是否使用'],
    ['冠状动脉旁路移植术' , '本次入院是否使用'],
    ['植入心脏除颤器' , '本次入院是否使用'],
    ['心脏射频消融术' , '本次入院是否使用'],
    ['PCI' , '本次入院是否使用'],
    ['冠脉造影' , '本次入院是否使用'],
    ['ARB' , '本次入院是否使用'],
    ['抗凝药物' , '本次入院是否使用'],
    ['ACEI' , '本次入院是否使用'],
    ['beta-blocker' , '本次入院是否使用'],
    ['强心药' , '本次入院是否使用'],
    ['抗血小板' , '本次入院是否使用'],
    ['钙通道阻滞剂' , '本次入院是否使用'],
    ['利尿剂' , '本次入院是否使用'],
    ['扩血管药' , '本次入院是否使用'],
    ['抗酸剂' , '本次入院是否使用'],
    ['他汀' , '本次入院是否使用'],
    ['高脂血症' , '本次入院诊断中是否包括'],
    ['心包疾病' , '本次入院诊断中是否包括'],
    ['脑梗/脑出血' , '本次入院诊断中是否包括'],
    ['糖尿病' , '本次入院诊断中是否包括'],
    ['心率失常' , '本次入院诊断中是否包括'],
    ['贫血' , '本次入院诊断中是否包括'],
    ['心力衰竭' , '本次入院诊断中是否包括'],
    ['瓣膜病' , '本次入院诊断中是否包括'],
    ['肾功能不全' , '本次入院诊断中是否包括'],
    ['甲亢或甲减' , '本次入院诊断中是否包括'],
    ['心肌梗死' , '本次入院诊断中是否包括'],
    ['先天性心脏病' , '本次入院诊断中是否包括'],
    ['高血压' , '本次入院诊断中是否包括'],
    ['心肌病' , '本次入院诊断中是否包括'],
    ['冠心病' , '本次入院诊断中是否包括'],
    ['脑利钠肽前体_偏低' , '下界0，上界150，单位pg/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['脑利钠肽前体_正常' , '下界0，上界150，单位pg/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['脑利钠肽前体_偏高' , '下界0，上界150，单位pg/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['丙氨酸氨基转移酶_偏低' , '下界0，上界40，单位U/L，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['丙氨酸氨基转移酶_正常' , '下界0，上界40，单位U/L，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['丙氨酸氨基转移酶_偏高' , '下界0，上界40，单位U/L，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['天冬氨酸氨基转移酶_偏低' , '下界0，上界40，单位U/L，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['天冬氨酸氨基转移酶_正常' , '下界0，上界40，单位U/L，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['天冬氨酸氨基转移酶_偏高' , '下界0，上界40，单位U/L，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['总蛋白_偏低' , '下界55，上界80，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['总蛋白_正常' , '下界55，上界80，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['总蛋白_偏高' , '下界55，上界80，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血清白蛋白_偏低' , '下界35，上界50，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血清白蛋白_正常' , '下界35，上界50，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血清白蛋白_偏高' , '下界35，上界50，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['总胆红素_偏低' , '下界0，上界21，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['总胆红素_正常' , '下界0，上界21，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['总胆红素_偏高' , '下界0，上界21，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['直接胆红素_偏低' , '下界0，上界8.6，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['直接胆红素_正常' , '下界0，上界8.6，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['直接胆红素_偏高' , '下界0，上界8.6，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['葡萄糖_偏低' , '下界3.4，上界6.1，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['葡萄糖_正常' , '下界3.4，上界6.1，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['葡萄糖_偏高' , '下界3.4，上界6.1，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['尿素_偏低' , '下界1.8，上界7.5，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['尿素_正常' , '下界1.8，上界7.5，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['尿素_偏高' , '下界1.8，上界7.5，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['肌酐_偏低' , '下界30，上界110，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['肌酐_正常' , '下界30，上界110，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['肌酐_偏高' , '下界30，上界110，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血清尿酸_偏低' , '下界104，上界444，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血清尿酸_正常' , '下界104，上界444，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血清尿酸_偏高' , '下界104，上界444，单位umol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['钠_偏低' , '下界130，上界150，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['钠_正常' , '下界130，上界150，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['钠_偏高' , '下界130，上界150，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['钙_偏低' , '下界2.09，上界2.54，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['钙_正常' , '下界2.09，上界2.54，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['钙_偏高' , '下界2.09，上界2.54，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['无机磷_偏低' , '下界0.89，上界1.6，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['无机磷_正常' , '下界0.89，上界1.6，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['无机磷_偏高' , '下界0.89，上界1.6，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['镁_偏低' , '下界0.6，上界1.4，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['镁_正常' , '下界0.6，上界1.4，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['镁_偏高' , '下界0.6，上界1.4，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['钾_偏低' , '下界3.5，上界5.5，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['钾_正常' , '下界3.5，上界5.5，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['钾_偏高' , '下界3.5，上界5.5，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['γ-谷氨酰基转移酶_偏低' , '下界0，上界50，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['γ-谷氨酰基转移酶_正常' , '下界0，上界50，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['γ-谷氨酰基转移酶' , '下界0，上界50，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['碱性磷酸酶_偏低' , '下界0，上界130，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['碱性磷酸酶_正常' , '下界0，上界130，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['碱性磷酸酶_偏高' , '下界0，上界130，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['肌酸激酶_偏低' , '下界2，上界200，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['肌酸激酶_正常' , '下界2，上界200，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['肌酸激酶_偏高' , '下界2，上界200，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['肌钙蛋白T_偏低' , '下界0，上界0.1，单位ng/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['肌钙蛋白T_正常' , '下界0，上界0.1，单位ng/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['肌钙蛋白T_偏高' , '下界0，上界0.1，单位ng/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['乳酸脱氢酶_偏低' , '下界40，上界250，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['乳酸脱氢酶_正常' , '下界40，上界250，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['乳酸脱氢酶_偏高' , '下界40，上界250，单位U/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['总胆固醇_偏低' , '下界3.1，上界5.7，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['总胆固醇_正常' , '下界3.1，上界5.7，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['总胆固醇_偏高' , '下界3.1，上界5.7，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['甘油三酯_偏低' , '下界0.4，上界1.7，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['甘油三酯_正常' , '下界0.4，上界1.7，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['甘油三酯_偏高' , '下界0.4，上界1.7，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['高密度脂蛋白胆固醇_偏低' , '下界1.0，上界1.6，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['高密度脂蛋白胆固醇_正常' , '下界1.0，上界1.6，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['高密度脂蛋白胆固醇_偏高' , '下界1.0，上界1.6，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['低密度脂蛋白胆固醇_偏低' , '下界0，上界3.4，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['低密度脂蛋白胆固醇_正常' , '下界0，上界3.4，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['低密度脂蛋白胆固醇_偏高' , '下界0，上界3.4，单位mmol/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['红细胞计数_偏低' , '下界4.3，上界5.9，单位10^12/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['红细胞计数_正常' , '下界4.3，上界5.9，单位10^12/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['红细胞计数_偏高' , '下界4.3，上界5.9，单位10^12/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血小板计数_偏低' , '下界100，上界300，单位10^9/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血小板计数_正常' , '下界100，上界300，单位10^9/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血小板计数_偏高' , '下界100，上界300，单位10^9/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血红蛋白测定_偏低' , '下界137，上界179，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血红蛋白测定_正常' , '下界137，上界179，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血红蛋白测定_偏高' , '下界137，上界179，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['红细胞比积测定_偏低' , '下界0.4，上界0.52，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['红细胞比积测定_正常' , '下界0.4，上界0.52，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['红细胞比积测定_偏高' , '下界0.4，上界0.52，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['红细胞体积分布宽度测定CV_偏低' , '下界0，上界14.5，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['红细胞体积分布宽度测定CV_正常' , '下界0，上界14.5，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['红细胞体积分布宽度测定CV_偏高' , '下界0，上界14.5，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['白细胞计数_偏低' , '下界3.5，上界10，单位10^9/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['白细胞计数_正常' , '下界3.5，上界10，单位10^9/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['白细胞计数_偏高' , '下界3.5，上界10，单位10^9/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['中性粒细胞_偏低' , '下界0.5，上界0.7，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['中性粒细胞_正常' , '下界0.5，上界0.7，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['中性粒细胞_偏高' , '下界0.5，上界0.7，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['淋巴细胞_偏低' , '下界0.2，上界0.4，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['淋巴细胞_正常' , '下界0.2，上界0.4，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['淋巴细胞_偏高' , '下界0.2，上界0.4，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['单核细胞_偏低' , '下界0.03，上界0.08，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['单核细胞_正常' , '下界0.03，上界0.08，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['单核细胞_偏高' , '下界0.03，上界0.08，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['嗜酸性粒细胞_偏低' , '下界0.01，上界0.05，单位pg/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['嗜酸性粒细胞_正常' , '下界0.01，上界0.05，单位pg/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['嗜酸性粒细胞_偏高' , '下界0.01，上界0.05，单位pg/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['嗜碱性粒细胞_偏低' , '下界0，上界0.01，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['嗜碱性粒细胞_正常' , '下界0，上界0.01，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['嗜碱性粒细胞_偏高' , '下界0，上界0.01，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['平均血小板体积测定_偏低' , '下界6.8，上界12.8，单位fl，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['平均血小板体积测定_正常' , '下界6.8，上界12.8，单位fl，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['平均血小板体积测定_偏高' , '下界6.8，上界12.8，单位fl，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['C-反应蛋白测定_偏低' , '下界0，上界0.8，单位mg/dl，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['C-反应蛋白测定_正常' , '下界0，上界0.8，单位mg/dl，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['C-反应蛋白测定_偏高' , '下界0，上界0.8，单位mg/dl，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['凝血酶时间测定_偏低' , '下界15，上界21，单位s，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['凝血酶时间测定_正常' , '下界15，上界21，单位s，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['凝血酶时间测定_偏高' , '下界15，上界21，单位s，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆凝血酶原时间测定_偏低' , '下界11，上界15，单位s，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆凝血酶原时间测定_正常' , '下界11，上界15，单位s，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆凝血酶原时间测定_偏高' , '下界11，上界15，单位s，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆凝血酶原活动度测定_偏低' , '下界70，上界150，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆凝血酶原活动度测定_正常' , '下界70，上界150，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆凝血酶原活动度测定_偏高' , '下界70，上界150，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆纤维蛋白原测定_偏低' , '下界2.0，上界4.0，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆纤维蛋白原测定_正常' , '下界2.0，上界4.0，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆纤维蛋白原测定_偏高' , '下界2.0，上界4.0，单位g/l，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆D-二聚体测定_偏低' , '下界0，上界0.5，单位ug/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆D-二聚体测定_正常' , '下界0，上界0.5，单位ug/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['血浆D-二聚体测定_偏高' , '下界0，上界0.5，单位ug/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['舒张压_偏低' , '下界60，上界90，单位mmHg，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['舒张压_正常' , '下界60，上界90，单位mmHg，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['舒张压_偏高' , '下界60，上界90，单位mmHg，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['射血分数_偏低' , '下界40，无上界，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['射血分数_正常' , '下界40，无上界，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['射血分数_偏高' , '下界40，无上界，单位%，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['收缩压_偏低' , '下界90，上界130，单位pg/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['收缩压_正常' , '下界90，上界130，单位pg/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['收缩压_偏高' , '下界90，上界130，单位pg/ml，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['心率_偏低' , '下界60，上界100，单位 次，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['心率_正常' , '下界60，上界100，单位 次，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['心率_偏高' , '下界60，上界100，单位 次，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['EGFR_偏低' , '下界30，上界90，单位 null，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['EGFR_正常' , '下界30，上界90，单位 null，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['EGFR_偏高' , '下界30，上界90，单位 null，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['年龄_偏低' , '下界60，上界80，单位 岁，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['年龄_正常' , '下界60，上界80，单位 岁，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['年龄_偏高' , '下界60，上界80，单位 岁，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['住院日_偏低' , '下界8，上界15，单位 日，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['住院日_正常' , '下界8，上界15，单位 日，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['住院日_偏高' , '下界8，上界15，单位 日，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['BMI_偏低' , '下界18，上界25，单位kg/m^2，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['BMI_正常' , '下界18，上界25，单位kg/m^2，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
    ['BMI_偏高' , '下界18，上界25，单位kg/m^2，默认值为0，低于下界将偏低置为1，上下界间将正常置为1，高于上界将偏高置为1'],
]};